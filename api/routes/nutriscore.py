"""
routes/nutriscore.py — Endpoints dm_nutriscore_by_country

TODO Phase 5 :
    GET /api/v1/nutriscore/            → liste paginée Nutri-Score par pays
    GET /api/v1/nutriscore/{country}   → détail d'un pays spécifique
    GET /api/v1/nutriscore/region/{region} → classement par région
    Authentification JWT requise sur tous les endpoints.
"""

# from fastapi import APIRouter, Depends, Query
# from api.auth import get_current_user
# from api.database import get_db

# router = APIRouter()

# TODO Phase 5 : implémenter les endpoints
